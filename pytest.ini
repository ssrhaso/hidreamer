[pytest]
testpaths    = tests
addopts      = -v --tb=short

markers =
    integration : tests that require real trained checkpoints / large data files
    gpu         : tests that require a CUDA-capable GPU
